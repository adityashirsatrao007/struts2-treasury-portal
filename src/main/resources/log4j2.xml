<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <Properties>
        <Property name="log.path">logs</Property>
        <Property name="log.pattern">[%d{yyyy-MM-dd HH:mm:ss.SSS}] [%t] %-5level %logger{36} - %msg%n</Property>
    </Properties>

    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${log.pattern}"/>
        </Console>
        
        <!-- Main Application Log -->
        <RollingFile name="AppFile" fileName="${log.path}/app.log" filePattern="${log.path}/app-%d{yyyy-MM-dd}.log">
            <PatternLayout pattern="${log.pattern}"/>
            <Policies>
                <TimeBasedTriggeringPolicy />
            </Policies>
        </RollingFile>

        <!-- Audit Log (Financial Transactions) -->
        <RollingFile name="AuditFile" fileName="${log.path}/audit.log" filePattern="${log.path}/audit-%d{yyyy-MM-dd}.log">
            <PatternLayout pattern="[%d{ISO8601}] %msg%n"/>
            <Policies>
                <TimeBasedTriggeringPolicy />
            </Policies>
        </RollingFile>

        <!-- Security Log (Login/Auth Failures) -->
        <RollingFile name="SecurityFile" fileName="${log.path}/security.log" filePattern="${log.path}/security-%d{yyyy-MM-dd}.log">
            <PatternLayout pattern="${log.pattern}"/>
            <Policies>
                <TimeBasedTriggeringPolicy />
            </Policies>
        </RollingFile>
    </Appenders>

    <Loggers>
        <!-- Audit Logger -->
        <Logger name="audit" level="info" additivity="false">
            <AppenderRef ref="AuditFile"/>
        </Logger>

        <!-- Security Logger -->
        <Logger name="security" level="info" additivity="false">
            <AppenderRef ref="SecurityFile"/>
        </Logger>

        <Root level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="AppFile"/>
        </Root>
    </Loggers>
</Configuration>
