<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Database Connection (HikariCP) -->
        <property name="hibernate.connection.provider_class">org.hibernate.hikaricp.internal.HikariCPConnectionProvider</property>
        <property name="hibernate.hikari.driverClassName">org.h2.Driver</property>
        <property name="hibernate.hikari.jdbcUrl">jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1</property>
        <property name="hibernate.hikari.username">sa</property>
        <property name="hibernate.hikari.password"></property>
        
        <!-- HikariCP Pool Settings -->
        <property name="hibernate.hikari.minimumIdle">5</property>
        <property name="hibernate.hikari.maximumPoolSize">20</property>
        <property name="hibernate.hikari.idleTimeout">300000</property>
        
        <!-- Hibernate Properties -->
        <property name="hibernate.dialect">org.hibernate.dialect.H2Dialect</property>
        <property name="hibernate.hbm2ddl.auto">update</property>
        <property name="hibernate.show_sql">false</property>
        <property name="hibernate.format_sql">true</property>
        <property name="hibernate.current_session_context_class">thread</property>
        <property name="hibernate.connection.release_mode">on_close</property>

        <!-- Disable the second-level cache  -->
        <property name="cache.provider_class">org.hibernate.cache.internal.NoCacheProvider</property>

        <property name="format_sql">true</property>

        <!-- Drop and re-create the database schema on startup (validate/update in production) -->
        <property name="hbm2ddl.auto">update</property>

        <!-- Entity Mapping -->
        <mapping class="com.demo.model.User"/>
        <mapping class="com.demo.model.Account"/>
        <mapping class="com.demo.model.Transfer"/>
        <mapping class="com.demo.model.AuditLog"/>
    </session-factory>
</hibernate-configuration>
